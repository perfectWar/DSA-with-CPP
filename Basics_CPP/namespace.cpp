#include<iostream>
using namespace std; //no need to write std:: again and again
int main(){
    cout << "Hii there!!!" << endl << "Nice to meet you!" << endl;

    // taking the input 
    int num;
    cin >> num;
    cout << "The value of num is : " << num << endl;

    // multiple input
    int x, y;
    cin >> x >> y;
    cout << "The value of x is : " << x << " and y is : " << y;

    return 0;
}