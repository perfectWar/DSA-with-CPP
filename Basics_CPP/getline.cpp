#include <iostream>
using namespace std;
int main(){
    string str;
    getline(cin, str); // take the full string in the line, 
    // after the enter it not going to take the inputs any more.
    // example input : we are going to do it 
    // get the car
    cout << str; 
    // ouput will be we are going to do it
    return 0;
}