#include<bits/stdc++.h>
using namespace std;

// deque stands for double ended queue 
// more flexible, as elements can be added and removed from both ends (at the front and the back)
// can also access elements by index numbers.

void deques(){
    deque<string> cars;

    cars.push_back("BMW");
    cout << cars[0];

    // all other function is same as list and vector 
}
int main(){
    deques();
}